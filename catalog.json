{
 "$schemaVersion": 2,
 "sdkBundles": [
  {
   "id": "servicenow-sdk",
   "displayName": "ServiceNow SDK (Fluent)",
   "shortLabel": "SDK",
   "description": "Bundled @servicenow/sdk + @servicenow/glide for authoring Flow Designer flows, Playbooks, Script Includes, ACLs, REST Messages, Connection Aliases, and more declaratively in TypeScript.",
   "version": "4.11.0",
   "glideVersion": "27.0.5",
   "tarballUrl": "https://github.com/PrimeDemonic/Nova_Catalog/releases/download/sdk-v4.11.0/sdk-bundle-4.11.0.tgz",
   "tarballSha256": "3c60353d9af40633f5e779441c919e4e140e525f9e25decd01617fb98b256fa7",
   "tarballs": {
    "darwin-arm64": {
     "tarballUrl": "https://github.com/PrimeDemonic/Nova_Catalog/releases/download/sdk-v4.11.0/sdk-bundle-4.11.0-darwin-arm64.tgz",
     "tarballSha256": "d1e87afe624232c3e8f0d8a3a34cbaac0127b440231bf7af56a5f88c34537133"
    }
   },
   "minimumGuiVersion": "3.1.0",
   "targetReleaseFamily": "yokohama",
   "releaseSummary": "Feature release over 4.10.0. NEW: TestSuite() API (define ATF Test Suites + their member tests in Fluent); GraphQLApi() API (scripted GraphQL on sys_graphql_schema — resolvers, type resolvers, secure-by-default ACL fields, SDL/resolver scripts via Now.include); Record() support for sys_ui_style Field Styles and schedule entries; official vite support incl. dev-server HMR (react-ui-page-vite-sample in sdk-examples); Playbooks: Playbook + Stage Level Permissions, optional activities, Set Playbook Outputs activity, Agentic Activity config on hybrid activities, On-Demand launcher configurations, input-dependent resolution, single-XML build output. FIXES: sys_ui_list lost on install (missing root XML attributes) + domain-on-list support; form sections breaking on rebuild when referencing the built-in Default view; table-level documentation dropped/merged into column docs; sys_dictionary reference fields (dictionary overrides) mis-serialised on update; Data Policy Rule default-state round-trip; unsafe delete instructions for table/column/index; UI Page apps loading legacy platform scripts that clobbered native Array.from (broke Radix overlay dismissal/keyboard nav — the root cause behind the mandatory polyfill in sn-fluent-ui-pages); Flow: doTheFollowingUntil redesigned + until-outside-loop validation, assignActionOutputs complex outputs, setFlowVariables inline scripts, Decimal in FlowObject/FlowArray, rawInputs on Action; Playbook: orphaned lanes/activities now deleted, input reference qualifiers kept on transform, startWithDelay durations, TemplateValue pills; AI Agent version details for legacy/migrated agents; NowAssist skill input/output name fallback; CLI surfaces fetch-failure causes and OAuth login no longer requests an id_token. VERIFIED on the bundled binary: now-sdk --version reports 4.11.0; CLI flag shapes unchanged from 4.10.0 (install --source / positional build [source])."
  }
 ],
 "servers": [
  {
   "id": "playwright",
   "displayName": "Playwright",
   "shortLabel": "PW",
   "description": "Drive a real browser for ServiceNow UI work — flow validation, workspace probing, portal smoke, dashboard verification, playbook UI checks. Per-project Chromium profile preserves saved passwords, history, and any persistent cookies (incl. SN 'Remember me' sessions when ticked at login).",
   "version": "0.0.79",
   "tarballUrl": "https://github.com/PrimeDemonic/Nova_Catalog/releases/download/playwright-v0.0.79/playwright-mcp-bundle-0.0.79.tgz",
   "tarballSha256": "5bf384073e06637e53bdceca3cef8599b3062408518e1f575265375a0b7a6bf0",
   "tarballs": {
    "darwin-arm64": {
     "tarballUrl": "https://github.com/PrimeDemonic/Nova_Catalog/releases/download/playwright-v0.0.79/playwright-mcp-bundle-0.0.79-darwin-arm64.tgz",
     "tarballSha256": "70a966ee95def228dcc3aefb4283b7a3b1e7af02dd6961b2d63b1f5fa1f72441"
    }
   },
   "mcpToolPrefix": "Playwright",
   "postInstallCommands": [
    {
     "cmd": "node",
     "args": [
      "./bin/install-browser.js",
      "chromium"
     ]
    }
   ],
   "requiresConfig": true,
   "configSchema": {
    "browser": {
     "type": "select",
     "options": [
      "chromium",
      "firefox",
      "webkit"
     ],
     "default": "chromium",
     "label": "Browser engine"
    },
    "headed": {
     "type": "boolean",
     "default": true,
     "label": "Show browser window (headed mode)",
     "description": "Headed mode lets you watch the automation. Disable for headless / CI."
    },
    "screenshotsDir": {
     "type": "path-relative-to-project",
     "default": "screenshots",
     "label": "Screenshots folder",
     "description": "Where Playwright saves screenshots. Path is relative to project root."
    },
    "profileDir": {
     "type": "path-relative-to-appdata",
     "default": "playwright-profiles/{projectId}",
     "label": "Browser profile folder",
     "description": "Persistent Chromium profile per project. Preserves saved passwords, history, localStorage, and any cookies the server flags as persistent. SN's default session cookie is session-only — tick 'Remember me' at login if you want auth to survive browser close. Per-project keeps SN instance sessions isolated."
    },
    "actionTimeoutMs": {
     "type": "select",
     "options": [
      "5000",
      "10000",
      "15000",
      "30000",
      "60000"
     ],
     "default": "15000",
     "label": "Action timeout (ms)",
     "description": "Client-side timeout for each Playwright action (click, screenshot, etc.). Playwright's own default of 5000ms is often too tight for ServiceNow UIs on corporate networks - screenshots wait on webfonts that can stall at a proxy."
    }
   },
   "minimumGuiVersion": "2.7.0",
   "useCases": [
    "Building or validating Flow Designer flows",
    "Workspace + UI Builder verification",
    "Portal page + theme smoke",
    "Dashboard / Performance Analytics visualisation review",
    "Playbook UI validation"
   ]
  },
  {
   "id": "nova-galaxy",
   "displayName": "Nova Galaxy",
   "shortLabel": "GX",
   "category": "tool",
   "description": "Interactive 3D galaxy visualization for Sentinel discovery data. Renders tables, artifacts, and relationships as an explorable solar system — tables are suns, artifacts orbit as planets, edges are gravitational links. Requires Sentinel cache data (run discover_table_ecosystem first).",
   "version": "0.5.0",
   "tarballUrl": "https://github.com/PrimeDemonic/Nova_Catalog/releases/download/nova-galaxy-v0.5.0/nova-galaxy-viz-0.5.0.tgz",
   "tarballSha256": "72e8198ea5609c809a488ad1350b4f5047d8c6464430b6788419204df6a9d2c5",
   "tarballs": {
    "darwin-arm64": {
     "tarballUrl": "https://github.com/PrimeDemonic/Nova_Catalog/releases/download/nova-galaxy-v0.5.0/nova-galaxy-viz-0.5.0.tgz",
     "tarballSha256": "72e8198ea5609c809a488ad1350b4f5047d8c6464430b6788419204df6a9d2c5"
    }
   },
   "mcpToolPrefix": "NovaGalaxy",
   "requiresConfig": true,
   "configSchema": {
    "bloomIntensity": {
     "type": "select",
     "options": [
      "off",
      "subtle",
      "medium",
      "intense"
     ],
     "default": "medium",
     "label": "Bloom / glow intensity",
     "description": "Controls the glow effect on nodes and particles"
    },
    "particleDensity": {
     "type": "select",
     "options": [
      "low",
      "medium",
      "high"
     ],
     "default": "medium",
     "label": "Particle density",
     "description": "Number of ambient particles. Lower for slower GPUs."
    },
    "cameraMode": {
     "type": "select",
     "options": [
      "orbit",
      "fly"
     ],
     "default": "orbit",
     "label": "Default camera mode",
     "description": "Orbit locks onto a focal point. Fly allows WASD free navigation."
    }
   },
   "minimumGuiVersion": "3.44.0",
   "useCases": [
    "Exploring a ServiceNow instance's table ecosystem in 3D",
    "Identifying configuration hotspots and sprawl visually",
    "Presenting instance architecture to stakeholders",
    "Comparing artifact density across tables"
   ]
  },
  {
   "id": "nova-recorder",
   "displayName": "Nova Recorder",
   "shortLabel": "REC",
   "category": "tool",
   "description": "Runtime bundle for Nova Recorder: narrated, annotated ServiceNow demo videos rendered from a project playbook (demos/<name>/demo.yaml) by the sn-demo-recorder skill. Ships ffmpeg 9.0.1 (gyan.dev release essentials build, GPL v3 - LICENSE and THIRD_PARTY.md included) plus the yaml parser the recorder tool needs. The browser comes from the Playwright catalog entry and narration from the platform's offline voice; no third-party service or key. This version is Windows-only (win32-x64) - the macOS bundle follows in a later catalog release.",
   "version": "9.0.1",
   "tarballUrl": "https://github.com/PrimeDemonic/Nova_Catalog/releases/download/nova-recorder-v9.0.1/nova-recorder-bundle-9.0.1.tgz",
   "tarballSha256": "afa6c9a63a718f9979c44bae65b0b26a87126aece743c89bb91ae65e0cee2445",
   "mcpToolPrefix": "NovaRecorder",
   "requiresConfig": false,
   "minimumGuiVersion": "3.96.0",
   "useCases": [
    "Recording a narrated, annotated demo of a ServiceNow feature from a plain-English brief",
    "Dry-running a demo playbook against the instance and fixing concerns before recording",
    "Re-voicing one segment in the Demos panel and re-rendering only that segment",
    "Producing a handover script (script.md) with review frames per segment"
   ]
  },
  {
   "id": "atlassian",
   "displayName": "Atlassian (Jira + Confluence)",
   "shortLabel": "JI",
   "description": "Official Atlassian Rovo remote MCP server - live Jira issue + Confluence page read/write under your own Atlassian sign-in (OAuth in-session, permissions follow your Atlassian roles). No install, no credentials stored by Nova: enable per project, then sign in once via /mcp (Claude) or codex mcp login (Codex). Lets sn-story-manager round-trip a live Jira backlog instead of exporting CSV.",
   "category": "mcp-server",
   "kind": "remote-http",
   "version": "1",
   "mcpToolPrefix": "atlassian",
   "remote": {
    "urlTemplate": "https://mcp.atlassian.com/v1/mcp/authv2",
    "oauth": {}
   },
   "requiresConfig": false,
   "minimumGuiVersion": "3.74.0",
   "useCases": [
    "Round-trip the project backlog to live Jira from sn-story-manager (CSV fallback retained)",
    "Read acceptance criteria / comments from Jira issues during builds",
    "Publish build notes or release summaries to Confluence pages"
   ]
  },
  {
   "id": "workiq-sharepoint",
   "displayName": "Work IQ SharePoint (preview)",
   "shortLabel": "SP",
   "description": "Microsoft's official remote MCP server for SharePoint (upload files, metadata, search, lists) via the Agent 365 tooling gateway. Actions run as the signed-in user (Entra). Requires: Microsoft 365 Copilot licence, and an Entra enterprise app registered with the matching WorkIQ-* API permission (admin consent). Get Tenant ID + App (client) ID from the Entra admin center. PREVIEW: Microsoft may rename server paths - if Test connection fails, verify the server path against learn.microsoft.com/microsoft-agent-365/tooling-servers-overview and correct it in Configure.",
   "category": "mcp-server",
   "kind": "remote-http",
   "version": "1",
   "mcpToolPrefix": "workiq_sharepoint",
   "remote": {
    "urlTemplate": "https://agent365.svc.cloud.microsoft/agents/tenants/{tenantId}/servers/{serverPath}",
    "oauth": {
     "clientIdFrom": "clientId",
     "callbackPort": 8080
    }
   },
   "requiresConfig": true,
   "configSchema": {
    "tenantId": {
     "type": "string",
     "default": "",
     "label": "Entra tenant ID",
     "description": "Directory (tenant) ID from the Microsoft Entra admin center"
    },
    "clientId": {
     "type": "string",
     "default": "",
     "label": "Entra app client ID",
     "description": "Application (client) ID of the registered enterprise app holding the WorkIQ-SharePointServer permission"
    },
    "serverPath": {
     "type": "string",
     "default": "mcp_SharePointTools",
     "label": "Server path",
     "description": "Work IQ server path segment (preview - confirm against the Microsoft Agent 365 docs if Test connection fails)"
    }
   },
   "minimumGuiVersion": "3.74.0",
   "useCases": [
    "Pull requirement docs / workshop notes from customer SharePoint during discovery",
    "Publish deliverables (UAT packs, release notes) to the engagement site"
   ]
  },
  {
   "id": "workiq-onedrive",
   "displayName": "Work IQ OneDrive (preview)",
   "shortLabel": "OD",
   "description": "Microsoft's official remote MCP server for the signed-in user's OneDrive (files and folders) via the Agent 365 tooling gateway. Actions run as the signed-in user (Entra). Requires: Microsoft 365 Copilot licence, and an Entra enterprise app registered with the matching WorkIQ-* API permission (admin consent). Get Tenant ID + App (client) ID from the Entra admin center. PREVIEW: Microsoft may rename server paths - if Test connection fails, verify the server path against learn.microsoft.com/microsoft-agent-365/tooling-servers-overview and correct it in Configure.",
   "category": "mcp-server",
   "kind": "remote-http",
   "version": "1",
   "mcpToolPrefix": "workiq_onedrive",
   "remote": {
    "urlTemplate": "https://agent365.svc.cloud.microsoft/agents/tenants/{tenantId}/servers/{serverPath}",
    "oauth": {
     "clientIdFrom": "clientId",
     "callbackPort": 8080
    }
   },
   "requiresConfig": true,
   "configSchema": {
    "tenantId": {
     "type": "string",
     "default": "",
     "label": "Entra tenant ID",
     "description": "Directory (tenant) ID from the Microsoft Entra admin center"
    },
    "clientId": {
     "type": "string",
     "default": "",
     "label": "Entra app client ID",
     "description": "Application (client) ID of the registered enterprise app holding the WorkIQ-OneDriveServer permission"
    },
    "serverPath": {
     "type": "string",
     "default": "mcp_OneDriveTools",
     "label": "Server path",
     "description": "Work IQ server path segment (preview - confirm against the Microsoft Agent 365 docs if Test connection fails)"
    }
   },
   "minimumGuiVersion": "3.74.0",
   "useCases": [
    "Read/write working documents in your own OneDrive from a build session"
   ]
  },
  {
   "id": "workiq-teams",
   "displayName": "Work IQ Teams (preview)",
   "shortLabel": "TM",
   "description": "Microsoft's official remote MCP server for Microsoft Teams (chats, channels, messages, members) via the Agent 365 tooling gateway. Actions run as the signed-in user (Entra). Requires: Microsoft 365 Copilot licence, and an Entra enterprise app registered with the matching WorkIQ-* API permission (admin consent). Get Tenant ID + App (client) ID from the Entra admin center. PREVIEW: Microsoft may rename server paths - if Test connection fails, verify the server path against learn.microsoft.com/microsoft-agent-365/tooling-servers-overview and correct it in Configure.",
   "category": "mcp-server",
   "kind": "remote-http",
   "version": "1",
   "mcpToolPrefix": "workiq_teams",
   "remote": {
    "urlTemplate": "https://agent365.svc.cloud.microsoft/agents/tenants/{tenantId}/servers/{serverPath}",
    "oauth": {
     "clientIdFrom": "clientId",
     "callbackPort": 8080
    }
   },
   "requiresConfig": true,
   "configSchema": {
    "tenantId": {
     "type": "string",
     "default": "",
     "label": "Entra tenant ID",
     "description": "Directory (tenant) ID from the Microsoft Entra admin center"
    },
    "clientId": {
     "type": "string",
     "default": "",
     "label": "Entra app client ID",
     "description": "Application (client) ID of the registered enterprise app holding the WorkIQ-TeamsServer permission"
    },
    "serverPath": {
     "type": "string",
     "default": "mcp_TeamsTools",
     "label": "Server path",
     "description": "Work IQ server path segment (preview - confirm against the Microsoft Agent 365 docs if Test connection fails)"
    }
   },
   "minimumGuiVersion": "3.74.0",
   "useCases": [
    "Post build/status updates to an engagement Teams channel",
    "Read stand-up threads for context during story work"
   ]
  }
 ]
}
